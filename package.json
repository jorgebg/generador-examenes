{
  "name": "generador-examenes",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && node scripts/make-offline.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:fixtures": "tsx scripts/gen-fixtures.mjs"
  },
  "dependencies": {
    "@picocss/pico": "^2.0.6",
    "expr-eval": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "marked": "^15.0.12",
    "marked-gfm-heading-id": "^4.1.2",
    "tsx": "^4.16.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5",
    "vite-plugin-singlefile": "^2.0.1"
  }
}
